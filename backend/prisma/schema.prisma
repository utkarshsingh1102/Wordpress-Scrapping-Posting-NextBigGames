generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Post {
  id                Int       @id @default(autoincrement())
  sourceUrl         String    @unique
  title             String
  slug              String
  author            String?
  publishedAt       DateTime?
  category          String?
  featuredImage     String?
  bodyHtml          String
  rewrittenBodyHtml String?
  imagesJson        String    @default("[]")
  linksJson         String    @default("[]")
  status            String    @default("scraped")
  wpPostId          Int?
  scrapedAt         DateTime  @default(now())
  wpPublishedAt     DateTime?
  originalFeaturedImage String?
  generatedImagePath String?
  generatedImagePrompt String?
  source            String?   @default("scrape")
  sourceSiteId      Int?
  sourceSite        Source?   @relation(fields: [sourceSiteId], references: [id], onDelete: SetNull)

  @@index([sourceSiteId])
}

model Source {
  id            Int       @id @default(autoincrement())
  name          String
  homepageUrl   String    @unique
  enabled       Boolean   @default(true)
  lastScrapedAt DateTime?
  createdAt     DateTime  @default(now())
  posts         Post[]
}

model LinkedinNewsletter {
  id            Int       @id @default(autoincrement())
  name          String
  url           String    @unique
  lastScrapedAt DateTime?
  createdAt     DateTime  @default(now())
}

model Job {
  id             Int       @id @default(autoincrement())
  type           String
  startedAt      DateTime  @default(now())
  finishedAt     DateTime?
  status         String
  postsFound     Int       @default(0)
  postsScraped   Int       @default(0)
  postsPublished Int       @default(0)
  postsSkipped   Int       @default(0)
  errorLog       String?
}

model Settings {
  id                  Int      @id @default(autoincrement())
  sourceUrl           String?
  wpUrl               String?
  wpUsername          String?
  wpAppPasswordEnc    String?
  wpAuthorUsername    String?
  anthropicApiKeyEnc  String?
  rewriteEnabled      Boolean?
  rewriteModel        String?
  openaiApiKeyEnc     String?
  scrapeCron          String?
  scrapeCronEnabled   Boolean?
  updatedAt           DateTime @updatedAt
}
