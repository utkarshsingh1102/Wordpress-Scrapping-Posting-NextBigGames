# In dev, leave empty — Vite proxies /api to http://localhost:3001 (see vite.config.ts).
# In production (Vercel), set this to your deployed backend's origin, no trailing slash.
# Example: VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=
